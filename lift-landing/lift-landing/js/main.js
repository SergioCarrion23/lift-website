/**
 * Inicialización: scroll suave ya vía CSS (scroll-behavior: smooth).
 * Asegura tema aplicado al cargar (theme-toggle.js lo hace).
 * Navegación por anclas ya funciona con enlaces #.
 */
(function () {
  document.body.classList.add('loaded');
})();
